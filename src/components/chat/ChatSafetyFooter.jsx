

export const ChatSafetyFooter = ({ _isPaid, _isClosed } = {}) => {
    // UI removida por solicitud del usuario para un look más profesional.
    // La lógica de isPaid e isClosed sigue intacta en el resto de la app.
    return null;
};

export default ChatSafetyFooter;
